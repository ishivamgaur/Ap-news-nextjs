export const metadata = {
  title: "Basketball News",
  description: "Latest Basketball news, scores, and updates from AP News.",
  openGraph: {
    title: "Basketball News | AP News",
    description: "Latest Basketball news, scores, and updates from AP News.",
  },
};

export default function BasketballLayout({ children }) {
  return <>{children}</>;
}
