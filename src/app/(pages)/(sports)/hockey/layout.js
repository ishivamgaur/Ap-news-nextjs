export const metadata = {
  title: "Hockey News",
  description: "Latest Hockey news, scores, and updates from AP News.",
  openGraph: {
    title: "Hockey News | AP News",
    description: "Latest Hockey news, scores, and updates from AP News.",
  },
};

export default function HockeyLayout({ children }) {
  return <>{children}</>;
}
